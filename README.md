### 📌 User Management API SPEC (Backend without DB)
This is a simple RESTful API built with Node.js and Express for managing users.
The project follows a clean folder structure using routes, controllers, and centralized error handling.
Currently, the application works without a database.
User data is stored temporarily in memory using an array.